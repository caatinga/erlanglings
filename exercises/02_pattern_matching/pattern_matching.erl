-module(pattern_matching).

-export([match_list/1, match_map/1, match_tuple/1]).

%% TODO: implement the functions bellow
match_tuple(T) ->
    undefined.

match_list(L) ->
    undefined.

match_map(M) ->
    undefined.
